# Локальный профиль: свои JWT (ES256) + Docker Postgres.
# Запуск: .\gradlew.bat bootRun --args="--spring.profiles.active=local"
#
# 1) Получить токен:  GET http://localhost:8080/dev/token
# 2) Проверить:      GET http://localhost:8080/me  с Authorization: Bearer <token>
#
# Админ из DataInitializer: sub=admin-seed-auth-id
#   http://localhost:8080/dev/token?sub=admin-seed-auth-id&email=admin@test.local

spring.datasource.url=jdbc:postgresql://localhost:5433/events
spring.datasource.username=dvizh
spring.datasource.password=dvizh
