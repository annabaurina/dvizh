# Локальная разработка без фронта и Supabase.
# Запуск: .\gradlew.bat bootRun --args='--spring.profiles.active=local'
# В запросах: -H "X-Dev-Auth-Id: admin-seed-auth-id"  (админ из DataInitializer)
# или любой другой id для обычного student (сначала GET /me).
app.dev-auth-enabled=true
